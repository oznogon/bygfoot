#include "bygfoot.h"

/* free everything we allocated */
void
free_memory(void);

/* free the history pointers */
void
free_history(void);

/* free the team and player histories */
void
free_histories(void);
